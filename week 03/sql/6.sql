SELECT albums.name AS "Name",albums.release_year AS "Release Year",SUM(songs.length) AS "Duration" FROM `albums` JOIN songs ON songs.album_id=albums.id GROUP By albums.id ORDER BY Duration DESC LIMIT 1;
